import React from 'react';
import * as rtl from '@testing-library/react';

import Display from './Display';

test('Display shows the right display value', () => {
    // const { getByText } = rtl.render(<Display />);
    // expect(getByText(/hello world!/i));
});
